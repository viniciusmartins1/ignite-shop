import { createStitches } from "@stitches/react";

export const {
  config,
  globalCss,
  css,
  createTheme,
  getCssText,
  keyframes,
  theme,
  styled,
} = createStitches({
  theme: {
    colors: {
      rocketseat: "#8257e6",
    },
  },
});
